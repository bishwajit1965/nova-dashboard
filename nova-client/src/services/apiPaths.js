export const API_PATHS = {
  USERS: {
    ENDPOINT: "users",
    KEY: ["users"],
  },
  ROLES: {
    ENDPOINT: "roles",
    KEY: ["roles"],
  },
  PERMISSIONS: {
    ENDPOINT: "permissions",
    KEY: ["permissions"],
  },
  SETTINGS: {
    ENDPOINT: "settings",
    KEY: ["siteSettings"],
  },

  // Extend this for other features
};

// export default API_PATHS;
